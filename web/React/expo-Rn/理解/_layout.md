### 一、_layout是什么
1、首先，_layout是一个组件，它共享样式与同一层级&&子层级的样式
比如我想在主页和其他页面使用相同的样式（比如顶部导航）；则可以在_layout组件中定义这些样式
最终，_layout组件会与其他组件结合使用，共享这些样式
可以把它理解成一个公共样式容器，提前封转好的组件，用于共享样式

2、_layout组件作用于同级及以下，如果有多个_layout组件，实行就近原则